%ul{:id => "packages", :title => "Packages", :selected => "true"}
  - @packages.each do |package|
    %li
      %a{ :href => "/sections/#{package.section.id}/packages/#{package.id}" }&= package.name.capitalize
  